[package]
name = "karukan-android"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
karukan-engine = { path = "../karukan-engine", default-features = false }
